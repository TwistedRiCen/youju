# AGENTS.md

## 1. 项目使命

有据（YouJu）帮助中国普通用户在关键事件中保存原始材料、整理事实、建立时间线、识别材料缺口，并导出可提交、可打印、可长期保存的材料包。

V0.1 只支持“网购商品存在问题且商家拒绝退款”场景。

本项目不是法律咨询、自动维权、投诉代办或商家曝光平台。

## 2. 必读文档

任何修改前，按顺序阅读：

1. `docs/superpowers/specs/2026-07-29-youju-v0.1-design.md`
2. 当前正在执行的 `docs/superpowers/plans/*.md`
3. 本文件 `AGENTS.md`
4. 与任务直接相关的包内 README、测试和规则文件

设计规格优先级高于实施计划，实施计划优先级高于临时口头假设。发现冲突时停止扩展实现，在结果中明确指出冲突。

## 3. 不可突破的产品边界

- 原始材料默认保存在用户设备，不默认上传业务服务器。
- 未配置 AI 时，核心流程必须完整可用。
- AI 只能生成候选内容，不能直接形成正式事实。
- 未经用户确认的 AI 内容不得进入正式 PDF、ZIP 或事实陈述。
- 每个 AI 候选事实必须关联来源文件；能够定位页码或区域时必须保存定位信息。
- API Key 默认只保存在当前会话内存，不得写入数据库、localStorage、日志、错误追踪或统计事件。
- 服务端不得长期保存用户事件、材料、事实、时间线或 API Key。
- 不生成法律结论、赔偿金额、成功率、胜诉率或“商家违法”等判断。
- 不自动登录第三方平台，不自动发送投诉，不建立曝光、评分或黑名单功能。
- 不增加劳动、租房、校园、医疗、债务等新场景，除非先完成新的设计评审。

## 4. 工程原则

### 4.1 先测试，后实现

所有行为变更遵循：

1. 写出最小失败测试；
2. 运行并确认测试因目标行为缺失而失败；
3. 编写最小实现；
4. 运行目标测试；
5. 运行受影响包测试；
6. 运行根级质量门禁；
7. 再提交。

不得先写实现后补一个只验证当前实现细节的测试。

### 4.2 最小改动

- 只修改当前任务明确要求的文件。
- 不顺手重构、不批量改名、不更换框架、不升级无关依赖。
- 文件职责单一；当一个文件同时承担领域规则、存储和 UI 职责时必须拆分。
- 优先使用纯函数、显式输入输出和可替换端口。

### 4.3 确定性优先

以下能力必须优先使用确定性代码：

- SHA-256 摘要；
- 规则检查；
- 状态流转；
- 时间线排序；
- 冲突检测；
- 导出前校验；
- PDF、CSV、HTML、ZIP 文件结构；
- 删除结果核验。

AI 不得替代上述确定性能力。

### 4.4 安全默认值

- 所有外部输入均视为不可信。
- 文件类型同时检查扩展名、MIME 和文件签名；不一致时拒绝或标记。
- 设置文件数量、单文件大小和总大小上限。
- 不记录完整请求体、文件内容、API Key 或模型原始敏感输入。
- 日志只使用事件 ID、分析版本 ID、错误码和耗时等低敏字段。
- 外部 AI 返回必须通过运行时 Schema 校验。
- 前端不得引入不必要的第三方脚本、分析 SDK 或远程资源。
- 新依赖必须说明必要性、许可证、维护状态和替代方案。

## 5. 技术基线

- Node.js 24 LTS。
- pnpm 10.34.5 workspace；首次初始化后使用 `pnpm-lock.yaml` 固化精确版本。
- TypeScript 严格模式。
- Web：Vue 3、Vite 8、PWA、Vitest、Playwright。
- API：Fastify 5，仅承担健康检查、规则版本和可选 AI 临时转发。
- 运行时数据校验：JSON Schema 优先；领域与 AI 契约必须同时具备静态类型和运行时校验。
- 用户界面文案使用简体中文；代码标识符、提交信息和技术日志使用英文。

不得擅自替换为 React、Nuxt、NestJS、Spring Boot、数据库或云对象存储。

## 6. 目录边界

- `apps/web`：用户界面、PWA 入口和浏览器适配层。
- `apps/api`：无状态服务端入口；不得实现业务数据持久化。
- `packages/domain`：领域实体、枚举、状态流转和跨模块契约。
- `packages/rule-engine`：规则 Schema、加载、校验和确定性评估。
- `packages/ai-core`：AI 请求/响应 Schema、候选内容和分析版本契约。
- `packages/evidence-*`：后续按摘要、存储、文件检查职责拆分。
- `packages/document-export`：后续负责导出模型和文件生成。
- `rules`：版本化规则数据，不放 UI 代码。
- `fixtures`：完全虚构的黄金案例及人工标准答案。
- `tests`：跨包集成、E2E、兼容性和安全回归测试。

包之间不得通过相对路径穿透源码；只能通过包公开入口导入。

## 7. 数据与状态规则

- ID 使用 UUID；测试中使用固定、可读 ID。
- 时间持久化为 ISO 8601 UTC 字符串；界面显示时再本地化。
- 金额使用整数分或十进制定点字符串，不使用二进制浮点表示人民币金额。
- `FactCandidate.reviewStatus` 只能为：`pending`、`confirmed`、`edited_and_confirmed`、`rejected`、`conflicted`。
- AI 分析结果只追加新 `AnalysisVersion`，不得覆盖历史版本。
- `ConfirmedFact` 只能由用户确认操作产生。
- 正式输出必须从 `ConfirmedFact`、用户手工确认内容和规则结果构建。

## 8. 测试要求

每个任务至少包含与其层级匹配的测试：

- 纯领域逻辑：Vitest Node 环境单元测试。
- 浏览器 API 适配：Vitest browser 或 Playwright，不用不真实的自制 DOM mock 掩盖兼容问题。
- API 路由：Fastify `inject()` 测试，不为单元测试启动真实端口。
- AI Provider：使用本地 mock server 或请求注入，不调用真实付费模型。
- E2E：至少覆盖 Chromium；关键本地存储和文件流程增加 WebKit。
- 黄金案例：所有规则、Schema 和预期输出可由命令行重复验证。

根级门禁最终统一为：

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm validate:fixtures
pnpm build
pnpm e2e
```

任务未涉及 E2E 且计划明确允许时，可以在开发循环中先运行局部命令；提交前必须运行计划指定的完整门禁。

## 9. 提交规则

- 每个计划任务至少一个独立提交。
- 提交前确认 `git diff --check` 无空白错误。
- 不提交 `.env`、API Key、真实用户材料、真实订单、真实聊天记录或构建产物。
- 使用 Conventional Commits：
  - `feat:` 新功能；
  - `fix:` 缺陷；
  - `test:` 测试；
  - `docs:` 文档；
  - `chore:` 工程配置；
  - `refactor:` 有测试保护且不改变行为的重构。

## 10. Codex 工作输出格式

每完成一个任务，必须报告：

1. 完成了什么；
2. 修改的文件；
3. 运行的命令及结果；
4. 尚存风险或限制；
5. 当前提交号；
6. 下一任务，但不要未经授权自动扩大范围。

不得用“应该通过”“大概率没问题”代替实际验证结果。
